{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio Mono-repo combining React Vite Frontend and Cloudflare Worker Backend",
  "scripts": {
    "install:all": "make install",
    "dev:pages": "make dev-pages",
    "dev:worker": "make dev-worker",
    "build:all": "make build"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
